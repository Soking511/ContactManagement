export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000', // Adjust if your API runs on a different URL
  // No need to specify socketPath here as it's defined in the contacts service
};
